// Code generated by entc, DO NOT EDIT.

package schema

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"
	"orginone/common/schema/baseinfoadministrativeareaall"
	"orginone/common/schema/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// BaseinfoadministrativeareaallQuery is the builder for querying Baseinfoadministrativeareaall entities.
type BaseinfoadministrativeareaallQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.Baseinfoadministrativeareaall
	// eager-loading edges.
	withParentx   *BaseinfoadministrativeareaallQuery
	withChildrens *BaseinfoadministrativeareaallQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BaseinfoadministrativeareaallQuery builder.
func (bq *BaseinfoadministrativeareaallQuery) Where(ps ...predicate.Baseinfoadministrativeareaall) *BaseinfoadministrativeareaallQuery {
	bq.predicates = append(bq.predicates, ps...)
	return bq
}

// Limit adds a limit step to the query.
func (bq *BaseinfoadministrativeareaallQuery) Limit(limit int) *BaseinfoadministrativeareaallQuery {
	bq.limit = &limit
	return bq
}

// Offset adds an offset step to the query.
func (bq *BaseinfoadministrativeareaallQuery) Offset(offset int) *BaseinfoadministrativeareaallQuery {
	bq.offset = &offset
	return bq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bq *BaseinfoadministrativeareaallQuery) Unique(unique bool) *BaseinfoadministrativeareaallQuery {
	bq.unique = &unique
	return bq
}

// Order adds an order step to the query.
func (bq *BaseinfoadministrativeareaallQuery) Order(o ...OrderFunc) *BaseinfoadministrativeareaallQuery {
	bq.order = append(bq.order, o...)
	return bq
}

// QueryParentx chains the current query on the "parentx" edge.
func (bq *BaseinfoadministrativeareaallQuery) QueryParentx() *BaseinfoadministrativeareaallQuery {
	query := &BaseinfoadministrativeareaallQuery{config: bq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(baseinfoadministrativeareaall.Table, baseinfoadministrativeareaall.FieldID, selector),
			sqlgraph.To(baseinfoadministrativeareaall.Table, baseinfoadministrativeareaall.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, baseinfoadministrativeareaall.ParentxTable, baseinfoadministrativeareaall.ParentxColumn),
		)
		fromU = sqlgraph.SetNeighbors(bq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildrens chains the current query on the "childrens" edge.
func (bq *BaseinfoadministrativeareaallQuery) QueryChildrens() *BaseinfoadministrativeareaallQuery {
	query := &BaseinfoadministrativeareaallQuery{config: bq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(baseinfoadministrativeareaall.Table, baseinfoadministrativeareaall.FieldID, selector),
			sqlgraph.To(baseinfoadministrativeareaall.Table, baseinfoadministrativeareaall.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, baseinfoadministrativeareaall.ChildrensTable, baseinfoadministrativeareaall.ChildrensColumn),
		)
		fromU = sqlgraph.SetNeighbors(bq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Baseinfoadministrativeareaall entity from the query.
// Returns a *NotFoundError when no Baseinfoadministrativeareaall was found.
func (bq *BaseinfoadministrativeareaallQuery) First(ctx context.Context) (*Baseinfoadministrativeareaall, error) {
	nodes, err := bq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{baseinfoadministrativeareaall.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bq *BaseinfoadministrativeareaallQuery) FirstX(ctx context.Context) *Baseinfoadministrativeareaall {
	node, err := bq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Baseinfoadministrativeareaall ID from the query.
// Returns a *NotFoundError when no Baseinfoadministrativeareaall ID was found.
func (bq *BaseinfoadministrativeareaallQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = bq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bq *BaseinfoadministrativeareaallQuery) FirstIDX(ctx context.Context) int64 {
	id, err := bq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Baseinfoadministrativeareaall entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Baseinfoadministrativeareaall entity is found.
// Returns a *NotFoundError when no Baseinfoadministrativeareaall entities are found.
func (bq *BaseinfoadministrativeareaallQuery) Only(ctx context.Context) (*Baseinfoadministrativeareaall, error) {
	nodes, err := bq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		return nil, &NotSingularError{baseinfoadministrativeareaall.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bq *BaseinfoadministrativeareaallQuery) OnlyX(ctx context.Context) *Baseinfoadministrativeareaall {
	node, err := bq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Baseinfoadministrativeareaall ID in the query.
// Returns a *NotSingularError when more than one Baseinfoadministrativeareaall ID is found.
// Returns a *NotFoundError when no entities are found.
func (bq *BaseinfoadministrativeareaallQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = bq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = &NotSingularError{baseinfoadministrativeareaall.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bq *BaseinfoadministrativeareaallQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := bq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Baseinfoadministrativeareaalls.
func (bq *BaseinfoadministrativeareaallQuery) All(ctx context.Context) ([]*Baseinfoadministrativeareaall, error) {
	if err := bq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return bq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (bq *BaseinfoadministrativeareaallQuery) AllX(ctx context.Context) []*Baseinfoadministrativeareaall {
	nodes, err := bq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Baseinfoadministrativeareaall IDs.
func (bq *BaseinfoadministrativeareaallQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := bq.Select(baseinfoadministrativeareaall.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bq *BaseinfoadministrativeareaallQuery) IDsX(ctx context.Context) []int64 {
	ids, err := bq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bq *BaseinfoadministrativeareaallQuery) Count(ctx context.Context) (int64, error) {
	if err := bq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return bq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (bq *BaseinfoadministrativeareaallQuery) CountX(ctx context.Context) int64 {
	count, err := bq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bq *BaseinfoadministrativeareaallQuery) Exist(ctx context.Context) (bool, error) {
	if err := bq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return bq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (bq *BaseinfoadministrativeareaallQuery) ExistX(ctx context.Context) bool {
	exist, err := bq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BaseinfoadministrativeareaallQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bq *BaseinfoadministrativeareaallQuery) Clone() *BaseinfoadministrativeareaallQuery {
	if bq == nil {
		return nil
	}
	return &BaseinfoadministrativeareaallQuery{
		config:        bq.config,
		limit:         bq.limit,
		offset:        bq.offset,
		order:         append([]OrderFunc{}, bq.order...),
		predicates:    append([]predicate.Baseinfoadministrativeareaall{}, bq.predicates...),
		withParentx:   bq.withParentx.Clone(),
		withChildrens: bq.withChildrens.Clone(),
		// clone intermediate query.
		sql:    bq.sql.Clone(),
		path:   bq.path,
		unique: bq.unique,
	}
}

// WithParentx tells the query-builder to eager-load the nodes that are connected to
// the "parentx" edge. The optional arguments are used to configure the query builder of the edge.
func (bq *BaseinfoadministrativeareaallQuery) WithParentx(opts ...func(*BaseinfoadministrativeareaallQuery)) *BaseinfoadministrativeareaallQuery {
	query := &BaseinfoadministrativeareaallQuery{config: bq.config}
	for _, opt := range opts {
		opt(query)
	}
	bq.withParentx = query
	return bq
}

// WithChildrens tells the query-builder to eager-load the nodes that are connected to
// the "childrens" edge. The optional arguments are used to configure the query builder of the edge.
func (bq *BaseinfoadministrativeareaallQuery) WithChildrens(opts ...func(*BaseinfoadministrativeareaallQuery)) *BaseinfoadministrativeareaallQuery {
	query := &BaseinfoadministrativeareaallQuery{config: bq.config}
	for _, opt := range opts {
		opt(query)
	}
	bq.withChildrens = query
	return bq
}

// ThenParentx tells the query-builder to eager-load the nodes that are connected to
// the "parentx" edge. The optional arguments are used to configure the query builder of the edge.
func (bq *BaseinfoadministrativeareaallQuery) ThenParentx(opts ...func(*BaseinfoadministrativeareaallQuery)) *BaseinfoadministrativeareaallQuery {
	query := &BaseinfoadministrativeareaallQuery{config: bq.config}
	for _, opt := range opts {
		opt(query.Where(baseinfoadministrativeareaall.IsDeleted(0)))
	}
	bq.withParentx = query
	return bq
}

// ThenChildrens tells the query-builder to eager-load the nodes that are connected to
// the "childrens" edge. The optional arguments are used to configure the query builder of the edge.
func (bq *BaseinfoadministrativeareaallQuery) ThenChildrens(opts ...func(*BaseinfoadministrativeareaallQuery)) *BaseinfoadministrativeareaallQuery {
	query := &BaseinfoadministrativeareaallQuery{config: bq.config}
	for _, opt := range opts {
		opt(query.Where(baseinfoadministrativeareaall.IsDeleted(0)))
	}
	bq.withChildrens = query
	return bq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Pid int64 `json:"pid"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Baseinfoadministrativeareaall.Query().
//		GroupBy(baseinfoadministrativeareaall.FieldPid).
//		Aggregate(schema.Count()).
//		Scan(ctx, &v)
//
func (bq *BaseinfoadministrativeareaallQuery) GroupBy(field string, fields ...string) *BaseinfoadministrativeareaallGroupBy {
	group := &BaseinfoadministrativeareaallGroupBy{config: bq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := bq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return bq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Pid int64 `json:"pid"`
//	}
//
//	client.Baseinfoadministrativeareaall.Query().
//		Select(baseinfoadministrativeareaall.FieldPid).
//		Scan(ctx, &v)
//
func (bq *BaseinfoadministrativeareaallQuery) Select(fields ...string) *BaseinfoadministrativeareaallSelect {
	bq.fields = append(bq.fields, fields...)
	return &BaseinfoadministrativeareaallSelect{BaseinfoadministrativeareaallQuery: bq}
}

func (bq *BaseinfoadministrativeareaallQuery) prepareQuery(ctx context.Context) error {
	for _, f := range bq.fields {
		if !baseinfoadministrativeareaall.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("schema: invalid field %q for query", f)}
		}
	}
	if bq.path != nil {
		prev, err := bq.path(ctx)
		if err != nil {
			return err
		}
		bq.sql = prev
	}
	return nil
}

func (bq *BaseinfoadministrativeareaallQuery) sqlAll(ctx context.Context) ([]*Baseinfoadministrativeareaall, error) {
	var (
		nodes       = []*Baseinfoadministrativeareaall{}
		_spec       = bq.querySpec()
		loadedTypes = [2]bool{
			bq.withParentx != nil,
			bq.withChildrens != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &Baseinfoadministrativeareaall{config: bq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("schema: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, bq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := bq.withParentx; query != nil {
		ids := make([]int64, 0, len(nodes))
		nodeids := make(map[int64][]*Baseinfoadministrativeareaall)
		for i := range nodes {
			fk := nodes[i].Pid
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(baseinfoadministrativeareaall.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "pid" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Parentx = n
			}
		}
	}

	if query := bq.withChildrens; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int64]*Baseinfoadministrativeareaall)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.Childrens = []*Baseinfoadministrativeareaall{}
		}
		query.Where(predicate.Baseinfoadministrativeareaall(func(s *sql.Selector) {
			s.Where(sql.InValues(baseinfoadministrativeareaall.ChildrensColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.Pid
			node, ok := nodeids[fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "pid" returned %v for node %v`, fk, n.ID)
			}
			node.Edges.Childrens = append(node.Edges.Childrens, n)
		}
	}

	return nodes, nil
}

func (bq *BaseinfoadministrativeareaallQuery) sqlCount(ctx context.Context) (int64, error) {
	_spec := bq.querySpec()
	_spec.Node.Columns = bq.fields
	if len(bq.fields) > 0 {
		_spec.Unique = bq.unique != nil && *bq.unique
	}
	c, err := sqlgraph.CountNodes(ctx, bq.driver, _spec)
	return int64(c), err
}

func (bq *BaseinfoadministrativeareaallQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := bq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("schema: check existence: %w", err)
	}
	return n > 0, nil
}

func (bq *BaseinfoadministrativeareaallQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   baseinfoadministrativeareaall.Table,
			Columns: baseinfoadministrativeareaall.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: baseinfoadministrativeareaall.FieldID,
			},
		},
		From:   bq.sql,
		Unique: true,
	}
	if unique := bq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := bq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, baseinfoadministrativeareaall.FieldID)
		for i := range fields {
			if fields[i] != baseinfoadministrativeareaall.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bq *BaseinfoadministrativeareaallQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bq.driver.Dialect())
	t1 := builder.Table(baseinfoadministrativeareaall.Table)
	columns := bq.fields
	if len(columns) == 0 {
		columns = baseinfoadministrativeareaall.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bq.sql != nil {
		selector = bq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bq.unique != nil && *bq.unique {
		selector.Distinct()
	}
	for _, p := range bq.predicates {
		p(selector)
	}
	for _, p := range bq.order {
		p(selector)
	}
	if offset := bq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BaseinfoadministrativeareaallGroupBy is the group-by builder for Baseinfoadministrativeareaall entities.
type BaseinfoadministrativeareaallGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bgb *BaseinfoadministrativeareaallGroupBy) Aggregate(fns ...AggregateFunc) *BaseinfoadministrativeareaallGroupBy {
	bgb.fns = append(bgb.fns, fns...)
	return bgb
}

// Scan applies the group-by query and scans the result into the given value.
func (bgb *BaseinfoadministrativeareaallGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := bgb.path(ctx)
	if err != nil {
		return err
	}
	bgb.sql = query
	return bgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := bgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(bgb.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := bgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) StringsX(ctx context.Context) []string {
	v, err := bgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = bgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) StringX(ctx context.Context) string {
	v, err := bgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(bgb.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := bgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) IntsX(ctx context.Context) []int {
	v, err := bgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = bgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) IntX(ctx context.Context) int {
	v, err := bgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(bgb.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := bgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := bgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = bgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) Float64X(ctx context.Context) float64 {
	v, err := bgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(bgb.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := bgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := bgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = bgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) BoolX(ctx context.Context) bool {
	v, err := bgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int64s returns list of int64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Int64s(ctx context.Context) ([]int64, error) {
	if len(bgb.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallGroupBy.Int64s is not achievable when grouping more than 1 field")
	}
	var v []int64
	if err := bgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Int64sX is like Int64s, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) Int64sX(ctx context.Context) []int64 {
	v, err := bgb.Int64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int64 returns a single int64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (bgb *BaseinfoadministrativeareaallGroupBy) Int64(ctx context.Context) (_ int64, err error) {
	var v []int64
	if v, err = bgb.Int64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallGroupBy.Int64s returned %d results when one was expected", len(v))
	}
	return
}

// Int64X is like Int64, but panics if an error occurs.
func (bgb *BaseinfoadministrativeareaallGroupBy) Int64X(ctx context.Context) int64 {
	v, err := bgb.Int64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (bgb *BaseinfoadministrativeareaallGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range bgb.fields {
		if !baseinfoadministrativeareaall.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := bgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (bgb *BaseinfoadministrativeareaallGroupBy) sqlQuery() *sql.Selector {
	selector := bgb.sql.Select()
	aggregation := make([]string, 0, len(bgb.fns))
	for _, fn := range bgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(bgb.fields)+len(bgb.fns))
		for _, f := range bgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(bgb.fields...)...)
}

// BaseinfoadministrativeareaallSelect is the builder for selecting fields of Baseinfoadministrativeareaall entities.
type BaseinfoadministrativeareaallSelect struct {
	*BaseinfoadministrativeareaallQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (bs *BaseinfoadministrativeareaallSelect) Scan(ctx context.Context, v interface{}) error {
	if err := bs.prepareQuery(ctx); err != nil {
		return err
	}
	bs.sql = bs.BaseinfoadministrativeareaallQuery.sqlQuery(ctx)
	return bs.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) ScanX(ctx context.Context, v interface{}) {
	if err := bs.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Strings(ctx context.Context) ([]string, error) {
	if len(bs.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := bs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) StringsX(ctx context.Context) []string {
	v, err := bs.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = bs.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) StringX(ctx context.Context) string {
	v, err := bs.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Ints(ctx context.Context) ([]int, error) {
	if len(bs.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := bs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) IntsX(ctx context.Context) []int {
	v, err := bs.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = bs.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) IntX(ctx context.Context) int {
	v, err := bs.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(bs.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := bs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) Float64sX(ctx context.Context) []float64 {
	v, err := bs.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = bs.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) Float64X(ctx context.Context) float64 {
	v, err := bs.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(bs.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := bs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) BoolsX(ctx context.Context) []bool {
	v, err := bs.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = bs.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) BoolX(ctx context.Context) bool {
	v, err := bs.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int64s returns list of int64s from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Int64s(ctx context.Context) ([]int64, error) {
	if len(bs.fields) > 1 {
		return nil, errors.New("schema: BaseinfoadministrativeareaallSelect.Int64s is not achievable when selecting more than 1 field")
	}
	var v []int64
	if err := bs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Int64sX is like Int64s, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) Int64sX(ctx context.Context) []int64 {
	v, err := bs.Int64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int64 returns a single int64 from a selector. It is only allowed when selecting one field.
func (bs *BaseinfoadministrativeareaallSelect) Int64(ctx context.Context) (_ int64, err error) {
	var v []int64
	if v, err = bs.Int64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{baseinfoadministrativeareaall.Label}
	default:
		err = fmt.Errorf("schema: BaseinfoadministrativeareaallSelect.Int64s returned %d results when one was expected", len(v))
	}
	return
}

// Int64X is like Int64, but panics if an error occurs.
func (bs *BaseinfoadministrativeareaallSelect) Int64X(ctx context.Context) int64 {
	v, err := bs.Int64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (bs *BaseinfoadministrativeareaallSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := bs.sql.Query()
	if err := bs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
