// Code generated by entc, DO NOT EDIT.

package schema

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"
	"orginone/common/schema/asmenu"
	"orginone/common/schema/asrole"
	"orginone/common/schema/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AsMenuQuery is the builder for querying AsMenu entities.
type AsMenuQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.AsMenu
	// eager-loading edges.
	withParentx   *AsMenuQuery
	withChildrens *AsMenuQuery
	withRoles     *AsRoleQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AsMenuQuery builder.
func (amq *AsMenuQuery) Where(ps ...predicate.AsMenu) *AsMenuQuery {
	amq.predicates = append(amq.predicates, ps...)
	return amq
}

// Limit adds a limit step to the query.
func (amq *AsMenuQuery) Limit(limit int) *AsMenuQuery {
	amq.limit = &limit
	return amq
}

// Offset adds an offset step to the query.
func (amq *AsMenuQuery) Offset(offset int) *AsMenuQuery {
	amq.offset = &offset
	return amq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (amq *AsMenuQuery) Unique(unique bool) *AsMenuQuery {
	amq.unique = &unique
	return amq
}

// Order adds an order step to the query.
func (amq *AsMenuQuery) Order(o ...OrderFunc) *AsMenuQuery {
	amq.order = append(amq.order, o...)
	return amq
}

// QueryParentx chains the current query on the "parentx" edge.
func (amq *AsMenuQuery) QueryParentx() *AsMenuQuery {
	query := &AsMenuQuery{config: amq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := amq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := amq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(asmenu.Table, asmenu.FieldID, selector),
			sqlgraph.To(asmenu.Table, asmenu.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, asmenu.ParentxTable, asmenu.ParentxColumn),
		)
		fromU = sqlgraph.SetNeighbors(amq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildrens chains the current query on the "childrens" edge.
func (amq *AsMenuQuery) QueryChildrens() *AsMenuQuery {
	query := &AsMenuQuery{config: amq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := amq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := amq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(asmenu.Table, asmenu.FieldID, selector),
			sqlgraph.To(asmenu.Table, asmenu.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, asmenu.ChildrensTable, asmenu.ChildrensColumn),
		)
		fromU = sqlgraph.SetNeighbors(amq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRoles chains the current query on the "roles" edge.
func (amq *AsMenuQuery) QueryRoles() *AsRoleQuery {
	query := &AsRoleQuery{config: amq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := amq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := amq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(asmenu.Table, asmenu.FieldID, selector),
			sqlgraph.To(asrole.Table, asrole.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, asmenu.RolesTable, asmenu.RolesPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(amq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AsMenu entity from the query.
// Returns a *NotFoundError when no AsMenu was found.
func (amq *AsMenuQuery) First(ctx context.Context) (*AsMenu, error) {
	nodes, err := amq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{asmenu.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (amq *AsMenuQuery) FirstX(ctx context.Context) *AsMenu {
	node, err := amq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AsMenu ID from the query.
// Returns a *NotFoundError when no AsMenu ID was found.
func (amq *AsMenuQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = amq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{asmenu.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (amq *AsMenuQuery) FirstIDX(ctx context.Context) int64 {
	id, err := amq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AsMenu entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AsMenu entity is found.
// Returns a *NotFoundError when no AsMenu entities are found.
func (amq *AsMenuQuery) Only(ctx context.Context) (*AsMenu, error) {
	nodes, err := amq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{asmenu.Label}
	default:
		return nil, &NotSingularError{asmenu.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (amq *AsMenuQuery) OnlyX(ctx context.Context) *AsMenu {
	node, err := amq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AsMenu ID in the query.
// Returns a *NotSingularError when more than one AsMenu ID is found.
// Returns a *NotFoundError when no entities are found.
func (amq *AsMenuQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = amq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = &NotSingularError{asmenu.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (amq *AsMenuQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := amq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AsMenus.
func (amq *AsMenuQuery) All(ctx context.Context) ([]*AsMenu, error) {
	if err := amq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return amq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (amq *AsMenuQuery) AllX(ctx context.Context) []*AsMenu {
	nodes, err := amq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AsMenu IDs.
func (amq *AsMenuQuery) IDs(ctx context.Context) ([]int64, error) {
	var ids []int64
	if err := amq.Select(asmenu.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (amq *AsMenuQuery) IDsX(ctx context.Context) []int64 {
	ids, err := amq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (amq *AsMenuQuery) Count(ctx context.Context) (int64, error) {
	if err := amq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return amq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (amq *AsMenuQuery) CountX(ctx context.Context) int64 {
	count, err := amq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (amq *AsMenuQuery) Exist(ctx context.Context) (bool, error) {
	if err := amq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return amq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (amq *AsMenuQuery) ExistX(ctx context.Context) bool {
	exist, err := amq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AsMenuQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (amq *AsMenuQuery) Clone() *AsMenuQuery {
	if amq == nil {
		return nil
	}
	return &AsMenuQuery{
		config:        amq.config,
		limit:         amq.limit,
		offset:        amq.offset,
		order:         append([]OrderFunc{}, amq.order...),
		predicates:    append([]predicate.AsMenu{}, amq.predicates...),
		withParentx:   amq.withParentx.Clone(),
		withChildrens: amq.withChildrens.Clone(),
		withRoles:     amq.withRoles.Clone(),
		// clone intermediate query.
		sql:    amq.sql.Clone(),
		path:   amq.path,
		unique: amq.unique,
	}
}

// WithParentx tells the query-builder to eager-load the nodes that are connected to
// the "parentx" edge. The optional arguments are used to configure the query builder of the edge.
func (amq *AsMenuQuery) WithParentx(opts ...func(*AsMenuQuery)) *AsMenuQuery {
	query := &AsMenuQuery{config: amq.config}
	for _, opt := range opts {
		opt(query)
	}
	amq.withParentx = query
	return amq
}

// WithChildrens tells the query-builder to eager-load the nodes that are connected to
// the "childrens" edge. The optional arguments are used to configure the query builder of the edge.
func (amq *AsMenuQuery) WithChildrens(opts ...func(*AsMenuQuery)) *AsMenuQuery {
	query := &AsMenuQuery{config: amq.config}
	for _, opt := range opts {
		opt(query)
	}
	amq.withChildrens = query
	return amq
}

// WithRoles tells the query-builder to eager-load the nodes that are connected to
// the "roles" edge. The optional arguments are used to configure the query builder of the edge.
func (amq *AsMenuQuery) WithRoles(opts ...func(*AsRoleQuery)) *AsMenuQuery {
	query := &AsRoleQuery{config: amq.config}
	for _, opt := range opts {
		opt(query)
	}
	amq.withRoles = query
	return amq
}

// ThenParentx tells the query-builder to eager-load the nodes that are connected to
// the "parentx" edge. The optional arguments are used to configure the query builder of the edge.
func (amq *AsMenuQuery) ThenParentx(opts ...func(*AsMenuQuery)) *AsMenuQuery {
	query := &AsMenuQuery{config: amq.config}
	for _, opt := range opts {
		opt(query.Where(asmenu.IsDeleted(0)))
	}
	amq.withParentx = query
	return amq
}

// ThenChildrens tells the query-builder to eager-load the nodes that are connected to
// the "childrens" edge. The optional arguments are used to configure the query builder of the edge.
func (amq *AsMenuQuery) ThenChildrens(opts ...func(*AsMenuQuery)) *AsMenuQuery {
	query := &AsMenuQuery{config: amq.config}
	for _, opt := range opts {
		opt(query.Where(asmenu.IsDeleted(0)))
	}
	amq.withChildrens = query
	return amq
}

// ThenRoles tells the query-builder to eager-load the nodes that are connected to
// the "roles" edge. The optional arguments are used to configure the query builder of the edge.
func (amq *AsMenuQuery) ThenRoles(opts ...func(*AsRoleQuery)) *AsMenuQuery {
	query := &AsRoleQuery{config: amq.config}
	for _, opt := range opts {
		opt(query.Where(asrole.IsDeleted(0)))
	}
	amq.withRoles = query
	return amq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ParentID int64 `json:"parentId,string"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AsMenu.Query().
//		GroupBy(asmenu.FieldParentID).
//		Aggregate(schema.Count()).
//		Scan(ctx, &v)
//
func (amq *AsMenuQuery) GroupBy(field string, fields ...string) *AsMenuGroupBy {
	group := &AsMenuGroupBy{config: amq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := amq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return amq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ParentID int64 `json:"parentId,string"`
//	}
//
//	client.AsMenu.Query().
//		Select(asmenu.FieldParentID).
//		Scan(ctx, &v)
//
func (amq *AsMenuQuery) Select(fields ...string) *AsMenuSelect {
	amq.fields = append(amq.fields, fields...)
	return &AsMenuSelect{AsMenuQuery: amq}
}

func (amq *AsMenuQuery) prepareQuery(ctx context.Context) error {
	for _, f := range amq.fields {
		if !asmenu.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("schema: invalid field %q for query", f)}
		}
	}
	if amq.path != nil {
		prev, err := amq.path(ctx)
		if err != nil {
			return err
		}
		amq.sql = prev
	}
	return nil
}

func (amq *AsMenuQuery) sqlAll(ctx context.Context) ([]*AsMenu, error) {
	var (
		nodes       = []*AsMenu{}
		_spec       = amq.querySpec()
		loadedTypes = [3]bool{
			amq.withParentx != nil,
			amq.withChildrens != nil,
			amq.withRoles != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &AsMenu{config: amq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("schema: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, amq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := amq.withParentx; query != nil {
		ids := make([]int64, 0, len(nodes))
		nodeids := make(map[int64][]*AsMenu)
		for i := range nodes {
			fk := nodes[i].ParentID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(asmenu.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "parent_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Parentx = n
			}
		}
	}

	if query := amq.withChildrens; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int64]*AsMenu)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.Childrens = []*AsMenu{}
		}
		query.Where(predicate.AsMenu(func(s *sql.Selector) {
			s.Where(sql.InValues(asmenu.ChildrensColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.ParentID
			node, ok := nodeids[fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "parent_id" returned %v for node %v`, fk, n.ID)
			}
			node.Edges.Childrens = append(node.Edges.Childrens, n)
		}
	}

	if query := amq.withRoles; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		ids := make(map[int64]*AsMenu, len(nodes))
		for _, node := range nodes {
			ids[node.ID] = node
			fks = append(fks, node.ID)
			node.Edges.Roles = []*AsRole{}
		}
		var (
			edgeids []int64
			edges   = make(map[int64][]*AsMenu)
		)
		_spec := &sqlgraph.EdgeQuerySpec{
			Edge: &sqlgraph.EdgeSpec{
				Inverse: false,
				Table:   asmenu.RolesTable,
				Columns: asmenu.RolesPrimaryKey,
			},
			Predicate: func(s *sql.Selector) {
				s.Where(sql.InValues(asmenu.RolesPrimaryKey[0], fks...))
			},
			ScanValues: func() [2]interface{} {
				return [2]interface{}{new(sql.NullInt64), new(sql.NullInt64)}
			},
			Assign: func(out, in interface{}) error {
				eout, ok := out.(*sql.NullInt64)
				if !ok || eout == nil {
					return fmt.Errorf("unexpected id value for edge-out")
				}
				ein, ok := in.(*sql.NullInt64)
				if !ok || ein == nil {
					return fmt.Errorf("unexpected id value for edge-in")
				}
				outValue := eout.Int64
				inValue := ein.Int64
				node, ok := ids[outValue]
				if !ok {
					return fmt.Errorf("unexpected node id in edges: %v", outValue)
				}
				if _, ok := edges[inValue]; !ok {
					edgeids = append(edgeids, inValue)
				}
				edges[inValue] = append(edges[inValue], node)
				return nil
			},
		}
		if err := sqlgraph.QueryEdges(ctx, amq.driver, _spec); err != nil {
			return nil, fmt.Errorf(`query edges "roles": %w`, err)
		}
		query.Where(asrole.IDIn(edgeids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := edges[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected "roles" node returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Roles = append(nodes[i].Edges.Roles, n)
			}
		}
	}

	return nodes, nil
}

func (amq *AsMenuQuery) sqlCount(ctx context.Context) (int64, error) {
	_spec := amq.querySpec()
	_spec.Node.Columns = amq.fields
	if len(amq.fields) > 0 {
		_spec.Unique = amq.unique != nil && *amq.unique
	}
	c, err := sqlgraph.CountNodes(ctx, amq.driver, _spec)
	return int64(c), err
}

func (amq *AsMenuQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := amq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("schema: check existence: %w", err)
	}
	return n > 0, nil
}

func (amq *AsMenuQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   asmenu.Table,
			Columns: asmenu.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt64,
				Column: asmenu.FieldID,
			},
		},
		From:   amq.sql,
		Unique: true,
	}
	if unique := amq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := amq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, asmenu.FieldID)
		for i := range fields {
			if fields[i] != asmenu.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := amq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := amq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := amq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := amq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (amq *AsMenuQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(amq.driver.Dialect())
	t1 := builder.Table(asmenu.Table)
	columns := amq.fields
	if len(columns) == 0 {
		columns = asmenu.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if amq.sql != nil {
		selector = amq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if amq.unique != nil && *amq.unique {
		selector.Distinct()
	}
	for _, p := range amq.predicates {
		p(selector)
	}
	for _, p := range amq.order {
		p(selector)
	}
	if offset := amq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := amq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AsMenuGroupBy is the group-by builder for AsMenu entities.
type AsMenuGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (amgb *AsMenuGroupBy) Aggregate(fns ...AggregateFunc) *AsMenuGroupBy {
	amgb.fns = append(amgb.fns, fns...)
	return amgb
}

// Scan applies the group-by query and scans the result into the given value.
func (amgb *AsMenuGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := amgb.path(ctx)
	if err != nil {
		return err
	}
	amgb.sql = query
	return amgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (amgb *AsMenuGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := amgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(amgb.fields) > 1 {
		return nil, errors.New("schema: AsMenuGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := amgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (amgb *AsMenuGroupBy) StringsX(ctx context.Context) []string {
	v, err := amgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = amgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (amgb *AsMenuGroupBy) StringX(ctx context.Context) string {
	v, err := amgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(amgb.fields) > 1 {
		return nil, errors.New("schema: AsMenuGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := amgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (amgb *AsMenuGroupBy) IntsX(ctx context.Context) []int {
	v, err := amgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = amgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (amgb *AsMenuGroupBy) IntX(ctx context.Context) int {
	v, err := amgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(amgb.fields) > 1 {
		return nil, errors.New("schema: AsMenuGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := amgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (amgb *AsMenuGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := amgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = amgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (amgb *AsMenuGroupBy) Float64X(ctx context.Context) float64 {
	v, err := amgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(amgb.fields) > 1 {
		return nil, errors.New("schema: AsMenuGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := amgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (amgb *AsMenuGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := amgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = amgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (amgb *AsMenuGroupBy) BoolX(ctx context.Context) bool {
	v, err := amgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int64s returns list of int64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Int64s(ctx context.Context) ([]int64, error) {
	if len(amgb.fields) > 1 {
		return nil, errors.New("schema: AsMenuGroupBy.Int64s is not achievable when grouping more than 1 field")
	}
	var v []int64
	if err := amgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Int64sX is like Int64s, but panics if an error occurs.
func (amgb *AsMenuGroupBy) Int64sX(ctx context.Context) []int64 {
	v, err := amgb.Int64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int64 returns a single int64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (amgb *AsMenuGroupBy) Int64(ctx context.Context) (_ int64, err error) {
	var v []int64
	if v, err = amgb.Int64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuGroupBy.Int64s returned %d results when one was expected", len(v))
	}
	return
}

// Int64X is like Int64, but panics if an error occurs.
func (amgb *AsMenuGroupBy) Int64X(ctx context.Context) int64 {
	v, err := amgb.Int64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (amgb *AsMenuGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range amgb.fields {
		if !asmenu.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := amgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := amgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (amgb *AsMenuGroupBy) sqlQuery() *sql.Selector {
	selector := amgb.sql.Select()
	aggregation := make([]string, 0, len(amgb.fns))
	for _, fn := range amgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(amgb.fields)+len(amgb.fns))
		for _, f := range amgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(amgb.fields...)...)
}

// AsMenuSelect is the builder for selecting fields of AsMenu entities.
type AsMenuSelect struct {
	*AsMenuQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ams *AsMenuSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ams.prepareQuery(ctx); err != nil {
		return err
	}
	ams.sql = ams.AsMenuQuery.sqlQuery(ctx)
	return ams.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (ams *AsMenuSelect) ScanX(ctx context.Context, v interface{}) {
	if err := ams.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Strings(ctx context.Context) ([]string, error) {
	if len(ams.fields) > 1 {
		return nil, errors.New("schema: AsMenuSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := ams.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (ams *AsMenuSelect) StringsX(ctx context.Context) []string {
	v, err := ams.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = ams.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (ams *AsMenuSelect) StringX(ctx context.Context) string {
	v, err := ams.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Ints(ctx context.Context) ([]int, error) {
	if len(ams.fields) > 1 {
		return nil, errors.New("schema: AsMenuSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := ams.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (ams *AsMenuSelect) IntsX(ctx context.Context) []int {
	v, err := ams.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = ams.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (ams *AsMenuSelect) IntX(ctx context.Context) int {
	v, err := ams.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(ams.fields) > 1 {
		return nil, errors.New("schema: AsMenuSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := ams.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (ams *AsMenuSelect) Float64sX(ctx context.Context) []float64 {
	v, err := ams.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = ams.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (ams *AsMenuSelect) Float64X(ctx context.Context) float64 {
	v, err := ams.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(ams.fields) > 1 {
		return nil, errors.New("schema: AsMenuSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := ams.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (ams *AsMenuSelect) BoolsX(ctx context.Context) []bool {
	v, err := ams.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = ams.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (ams *AsMenuSelect) BoolX(ctx context.Context) bool {
	v, err := ams.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int64s returns list of int64s from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Int64s(ctx context.Context) ([]int64, error) {
	if len(ams.fields) > 1 {
		return nil, errors.New("schema: AsMenuSelect.Int64s is not achievable when selecting more than 1 field")
	}
	var v []int64
	if err := ams.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Int64sX is like Int64s, but panics if an error occurs.
func (ams *AsMenuSelect) Int64sX(ctx context.Context) []int64 {
	v, err := ams.Int64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int64 returns a single int64 from a selector. It is only allowed when selecting one field.
func (ams *AsMenuSelect) Int64(ctx context.Context) (_ int64, err error) {
	var v []int64
	if v, err = ams.Int64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{asmenu.Label}
	default:
		err = fmt.Errorf("schema: AsMenuSelect.Int64s returned %d results when one was expected", len(v))
	}
	return
}

// Int64X is like Int64, but panics if an error occurs.
func (ams *AsMenuSelect) Int64X(ctx context.Context) int64 {
	v, err := ams.Int64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (ams *AsMenuSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ams.sql.Query()
	if err := ams.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
